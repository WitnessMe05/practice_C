#include <iostream>

int main ()
{
    using namespace std;

    int count = 0;
    // for ( ;count < 10; ++count)
    // while (true)
    for(;true;++count) // for (; ; ++count)
    {
        cout << count << endl;
    }

    cout << count << endl;

}